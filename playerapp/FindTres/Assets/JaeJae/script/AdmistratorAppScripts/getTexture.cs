using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using UnityEditor;

public class getTexture : MonoBehaviour
{
    Texture2D PreviewRawI;

    // Start is called before the first frame update
    void Start()
    {
        //PreviewRawI = GameObject.Find("Preview").GetComponent<Texture2D>();
    }

    public void putTexture()
    {
        //PreviewRawI=Camera1.Instance.savess;
    }
}
